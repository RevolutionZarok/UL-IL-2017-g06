\subsubsection{subfunction-oeSendStatistic}

\label{RE-use-case-oeSendStatistic}


Send the statistic to the system that the system send it to the administrator		  


\begin{usecase}
  \addheading{Use-Case Description}
  \addsingletwocolumnrow{Name}{oeSendStatistic}
  \addsingletwocolumnrow{Scope}{system}
  \addsingletwocolumnrow{Level}{subfunction}
  

\addrowheading{Primary actor(s)}
\addnumberedsinglerow{}{\msrcode{actSystem[active]}}


\addrowheading{Secondary actor(s)}
\addnumberedsinglerow{}{\msrcode{actAdministrator[passive]}}
\addnumberedsinglerow{}{\msrcode{actDatabase[passive]}}

\addrowheading{Goal(s) description}
\addsinglerow{Send the statistic to the system that the system send it to the administrator}

\addrowheading{Protocol condition(s)}
\addnumberedsinglerow{}{The actor has been login as a administrator to call the statistic so the subfunction can send the data to the system 
}

\addrowheading{Pre-condition(s)}
\addnumberedsinglerow{}{The administrator has call the statistic, so the function will send the information
}

\addrowheading{Main post-condition(s)}
\addnumberedsinglerow{}{if the administrator click to the button statistic so the system will find the information.
}
\addnumberedsinglerow{}{the Database has send back the information to the system and after to the actor. 
}

\addrowheading{Additional Information}
\addsinglerow{
none
}

\end{usecase} 

